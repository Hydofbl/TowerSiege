using UnityEngine;

public class NormyUnit : BaseUnitType
{
    public NormyUnit(int health, float movementSpeed, int cost, Vector3 direction) : base(health, movementSpeed, cost, direction)
    {
    }
}
